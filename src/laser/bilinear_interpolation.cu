#include "laser/bilinear_interpolation.cuh"

namespace tenryu::laser {

// Intentionally empty: bilinear interpolation helpers are header-only
// for host/device inlining and ODR-safe use from multiple CUDA translation units.

}  // namespace tenryu::laser

