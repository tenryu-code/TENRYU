# TENRYU β 更新履歴

配布スナップショットの更新記録です。日付はスナップショット作成日。

## 2026-09-26

配布物のビルドの不具合と、1D の再開・MPI の不具合（MPI の複数ランクでの電子の熱伝導の陰解法と、スナップショット・チェックポイント・履歴の出力を含む）を直し、互換エネルギー方式を低温平衡構成則と併用できるようにし、キーを読むだけで何もしていなかったイオン熱伝導を 1D に実装しました。同梱のデッキの 1 ランクでの結果は、512 セル以上の 1D の 2 温度のデッキ（`examples/implosion/gxii_square_120J_6ns_fld_nr600.py`、`gxii_square_240J_12ns_fld_nr1500.py`）が状態方程式の閉包の核関数の選択により丸め誤差の程度で変わるほかは変わりません（燃焼のある run を途中から再開した場合は、再開後の燃焼の集計が元の run と最下位ビットまで一致するようになります。MPI の複数ランクで電子の熱伝導の陰解法を使う run の結果は変わります）。

### 機能追加

- **イオン熱伝導（`Numerics.conduction.ion_conduction=True`）を 1D の 2 温度で実装しました。これまでこのキーは読み込むだけで何もしておらず、仕様書と公開ドキュメントは存在しない実装（2D の Kershaw の演算子を使う）を説明していました。** 電子の熱伝導の後に、Braginskii のイオン熱伝導率 κ_i = 3.906 n_i T_i τ_i / m_i（衝突時間と Coulomb 対数はイオン粘性と同じ NRL の式）でイオン温度を後退 Euler の三重対角系として 1 回解き、解いた温度の面の熱流を両隣のセルへ逆符号で同じ値だけ入れ（イオンのエネルギーの総和は解法の残差によらず丸め誤差まで保存）、流体と同じ状態方程式の閉包でイオンの温度と圧力を閉じ直します。面の流束はイオンの自由流束 n_i T_i (T_i/m_i)^(1/2) の `ion_f_lim` 倍（新しいキー、既定 1）で制限します。NIF DS デッキ（配布物外）では、最初の衝撃波が中心に収束するときに生まれる中性子の数が格子を細かくするほど増え続けていましたが、中心のセルも半分ずつ細かくする格子の系列では、イオン熱伝導なしでは発散的に増え（1.21e5、3.76e5、6.16e5）、イオン熱伝導ありでは収束します（1.15e5、2.64e5、3.34e5、3.67e5、3.89e5。外挿で約 4.1〜4.25e5）。以前の既定の格子では中性子の 2/3 が最内の 1 セルで生まれていました。このデッキの既定をイオン熱伝導ありと、この系列の 4 段目の格子（燃料 1600・殻 1280 セル）にしました（14 ns の DD 中性子数は 1.2259e5 から 3.756e5 に。RTX 4090 で 11 分）。
- GUI（TENRYU Studio）のイオン熱伝導の切り替えを、ソルバーが受け付ける 1D・2 温度に合わせました（2D や 1 温度ではフォームのエラーになり、デッキを生成しません）。有効にすると制限係数 `ion_f_lim`（既定 1）の入力欄が出ます。デッキの取り込みも `ion_f_lim` を読みます。
- 1D の燃焼のスナップショットとチェックポイントに `hydro/burn_neutron_cum`（セルで生まれた中性子の累積数、DD と DT の中性子分枝）を追加しました。中性子がどこで生まれたかが分かります（`burn_eps_cum` は荷電生成物のエネルギーの沈着先です）。
- **互換エネルギー方式（`Numerics.hydro.compatible_energy=True`）を低温平衡構成則（`T_start_inactive_cells="cold_equilibrium"`）と併用できるようにしました。** 低温平衡のセルでは電子の力の圧力（電子側の自由エネルギーの弾性の力を含む）の仕事を電子へ、残りを他の種へ入れ、電子の熱量座標から密度変化による cold エネルギーの変化を返すので、流体の更新は全エネルギーを丸め誤差まで保存します（閉じた球の試験で 50 step の全エネルギーの変化が 3.3e10 erg に対し 7.6e-6 erg、互換でない更新では 9.3e3 erg）。あわせて、エネルギーを正とする閉包（`eos_closure_mode="energy_authoritative"`）では、表のエネルギーが一部の密度行で温度に対して減少していても互換エネルギー方式を受け付けます（閉包がエネルギーを書き戻さないので保存は崩れません。従来の閉包では引き続き拒否します）。NIF DS デッキ（配布物外）の既定を互換エネルギー方式にし、エネルギー保存誤差の最大は 3.4e-4（駆動の立ち上がり）から 5.1e-6（FLD の外側反復の許容誤差による振動）に下がり、14 ns の DD 中性子数は 1.2467e5 → 1.2259e5（−1.7 %）で、CFL を半分・四分の一にしても 0.2 % 以内です。低温平衡でない run の互換エネルギー更新は変わりません（GXII の FLD デッキ 300 ステップの履歴がビット一致）。

### 互換性に関わる変更

- `Numerics.conduction.ion_conduction=True` を 2D、1 温度、物質別の保存（`per_material_conservation_enabled=True`）、Mie–Grüneisen の流体 EOS と組み合わせたデッキは設定エラーになります（以前はキーが無視されていました）。`ion_f_lim` は正の有限値が必要です。

### 高速化

- 保存形の放射のセル移流（`multigroup_diffusion.hydro_coupling="conservative_advection"`）を使う 1D の run で、毎ステップのデバイスメモリの確保と解放（解放はデバイス全体を同期させる）をなくしました（結果はビット一致）。
- A100 でも、光線のレーン数の自動選択（`Laser.raytrace.lanes_per_ray=0`）が最速か 2 % 以内の幅を選ぶことを確かめました（GXII の FLD デッキ、光線 200・1000・4000 本）。
- イオン熱伝導の三重対角系は、640 セル以下の線はホストとの同期なしに 1 ブロックの並列巡回縮約で（360 セルで 1 回 20.9 µs）、それより長い線は cuSPARSE の gtsv2 で解きます（1 ブロックの縮約は倍精度の計算が 1 つの SM に集まり、2880 セルで 221 µs、gtsv2 は 60 µs）。
- 1D の 2 温度の状態方程式の閉包は、512 セル以上の線では 1 セル 1 スレッドの核関数を使います（1 セル 2 warp の核関数はセル数に比例して時間が延び、2880 セルで 1 ステップ 1.62 ms、1 スレッドの核関数は 0.40 ms。512 セル未満は従来どおり 2 warp。両者の違いは丸め誤差の程度）。NIF DS デッキ（配布物外）の既定の格子（2880 セル）で 1 ステップ 6.7 → 5.8 ms。1 回ごとのエネルギー保存の検査は、環境変数 `TENRYU_ION_CONDUCTION_AUDIT=1` のときだけ行います。
- 1D の熱伝導のセルごと・面ごとの倍精度の核関数（Spitzer の係数、Kirchhoff の時間刻み比、流束制限、三重対角系の組み立て、下限の処理）を 1 ブロック 32 スレッドで起動し、1D の数百セルの計算を多くの SM に分けました（結果はビット一致）。NIF DS の 8 ns 以降の再開（他の run と GPU を共有した測定）で、イオン熱伝導を入れた 1 step の核関数の時間が 1.869 → 1.707 ms になり、イオン熱伝導なしの以前の 1.729 ms を下回りました。

### 不具合修正

- **MPI の複数ランクで、1D の電子の熱伝導の陰解法（`Numerics.conduction.solver="implicit"`）の結果がランク数によって変わっていた。** 線全体の三重対角系を各ランクで同じように解く設計ですが、熱伝導率と熱容量の係数を自分の持ち分のセルと両隣 1 セルでしか計算しておらず（他のセルは作業領域に残っていた値）、解く前に線全体を持ち主の値にそろえてもいませんでした（放射のあるデッキでは直前の放射の段がそろえていましたが、係数の範囲の欠陥は同じです）。係数を線全体で計算し、解く前に線全体を持ち主の値で集めるようにしました。放射なしで温度がランク境界をまたいで変わる試験デッキでは、修正前は 2 ランクで 1 step 後に境界付近の Te が相対 7e-7、25 step 後に 1e-5 ずれていました（修正後はビット一致）。放射のある NIF DS デッキ（配布物外、6 ns）の 2 ランクの DD 中性子数は、1 ランクとの相対差が 1.8e-8 でした（次の項目の修正と合わせて 1・2・4 ランクで一致）。1 ランクの結果は変わりません（GXII の FLD デッキ 300 step と NIF DS の 1 ns の履歴がビット一致）。
- **MPI の複数ランクの 1D の run で、スナップショット・チェックポイント・履歴の一部が、ランク 0 以外の担当セルの古い値を書いていた。** 1D では各ランクが全長の配列を持ち、流体や状態方程式の閉包などは自分の担当セルだけを更新しますが、出力はランク 0 が全長の配列から書いており、出力前に担当セルの値を集めていませんでした。スナップショットとチェックポイントのランク 1 以降の担当域が古く（このチェックポイントから再開すると、その域が古い値で戻ります）、履歴の放射場のエネルギー・エネルギー保存誤差・殻の半径・最大密度・時間刻みの内訳が 1 ランクと違い、互換エネルギー方式の残差の検査はランク境界の節点を二重に数えて毎ステップ警告を出していました。スナップショットとチェックポイントの全エネルギー（`time_state/E_*`）は、ランク 0 の担当セルだけの和でした。状態も途中から丸め誤差の程度で分かれていました。各ステップの終わりと最初の出力の前に全ランクで担当セルの値を集めるようにし、NIF DS デッキ（6 ns）の 1・2・4 ランクでスナップショットの全データがビットで一致し、DD 中性子数も一致するようになりました（履歴の和は足す順序による丸めの差だけ）。以前に MPI の複数ランクで書いた 1D のチェックポイントからの再開は勧めません。
- **BUILD.md の手順どおりのビルドが、CUDA を標準の場所（`/usr/local/cuda`）に入れた計算機で失敗していた。** 純 C++ のソース（`src/mesh/z_reflection.cpp` など）をホストのコンパイラで翻訳するときに、Thrust のヘッダ（`thrust/device_vector.h`）が見つかりませんでした（CUDA の include ディレクトリがコンパイラの既定の探索パスにある計算機でだけ通っていた）。CUDA の include ディレクトリを、コアのライブラリから依存するすべてのターゲットへ渡すようにしました。
- **燃焼のある 1D の run を途中のチェックポイントから再開すると、燃焼の集計（放出エネルギー・沈着・逃散・中性子の診断）が元の run と最下位ビットで一致しなかった。** チェックポイントは燃料の比インベントリ（1 g あたりの粒子数）を数密度に直して保存し、再開時に密度で割って戻していたため、戻した値が最下位ビットでずれていました。比インベントリをそのまま `burn_state/specific_inventory` にも保存し、再開はこれを使います（無い以前のチェックポイントは従来どおり数密度から戻します）。DD 燃焼のあるデッキを 1 ns と 7 ns から再開した run が、元の run と履歴の全列でビット一致します。
- **MPI の複数ランクで 1D の run が起動時に止まっていた（2026-07-29 以降）。** 2D の時間積分の選択 `Numerics.hydro.time_integration`（既定 `"midpoint_v1"`）に対する MPI 非対応の検査が 1D にもかかっていました。この選択は 2D でしか使わないので、検査を 2D に限りました。1D の MPI の並列試験（2・4 ランクの最終状態の全場を 1 ランクの run とビットで比較。FLD・S_N・空隙・燃焼・GXII）がすべて通ります。
- 再開したときに、初期状態向けの「材料ごとの初期全圧の不一致」の警告を出していました（実行中の状態では材料ごとの圧力が違って当然で、参照状態をそろえる助言も当てはまりません）。

### ドキュメント・検証系

- 公開ドキュメント（日英）の熱伝導のページのイオン熱伝導の説明を実装に合わせて書き直しました（2D で Kershaw の演算子を使うとしていた記述は誤り）。公開ドキュメントの MPI のページの試験群の数を 14 件にし、1D の出力の集約を追記。
- 公開ドキュメント（日英）の再開のビット一致の記述を、確かめた範囲（GXII の FLD・S_N デッキ、燃焼のあるデッキ）と対象のチェックポイントに合わせて更新。
- 新しい試験: `test_hydro_1d_step` の `[closure]` に、低温平衡の 2 つの表材料と、表材料と理想気体の組み合わせ（セルごとに方式を切り替える経路）を追加（各セルが自分の材料の表と cold curve を使うことを、1 スレッド版と独立に確かめる）。`test_hdf5_roundtrip` に燃焼の比インベントリのチェックポイントの往復（ビット一致）。`test_hydro_1d_step` の `[compatible]` に、低温平衡の閉じた球の全エネルギーの保存と、温度に対して減少する行を持つ表の受理（エネルギーを正とする閉包で受け付け、従来の閉包で拒否）。`test_conduction_ion_1d`（イオン熱伝導: 平面の固有モードの減衰率を Braginskii の式から計算した値と 1e-5 で比較、球の高温点でのエネルギー保存と最大値原理、温度の段差での自由流束による上限、void セルでの断熱、2 セルの平衡）。燃焼の GPU とホストの一致試験に、セルごとの中性子の数の一致と総和の検査。MPI の並列試験 `parallel_gate_burn1d_conduction_implicit_np2`・`parallel_gate_burn1d_conduction_sts_np2`（電子の熱伝導の陰解法または STS とイオン熱伝導。温度がランク境界をまたいで直線的に変わり Ti = Te/2。2 ランクの最終状態の全場が 1 ランクとビット一致。陰解法の試験は修正前の係数の範囲と集約なしのどちらでも失敗し、STS の試験はイオン熱伝導の集約を外すと失敗することを確かめた）。MPI のスナップショットの試験 `parallel_snapshot_gate_burn1d_conduction_implicit_np2`・`parallel_snapshot_gate_gxii1d_np2`（2 ランクのスナップショットの全数値データが 1 ランクとビット一致。修正前の版では失敗）。GXII の MPI 試験の step 数を 2 から 50 に。GUI のイオン熱伝導の生成・入力検査・取り込みの往復の試験。

## 2026-09-25

1D の残存課題（多材料の扱い、未完成だったカーネル、レイトレースの精度、S_N の空間離散化、FLD の外側反復の収束）を修正し、結果を変えない高速化を加えました。1D の本番デッキの結果が変わります（基準版 2026-09-24 との比較: CD の表を使う GXII の FLD デッキ（2.5 ns）は吸収率 0.3107 → 0.3111、最大密度 48.63 → 48.66 g/cm³ でほぼ変わらず、エネルギー保存誤差の最大は 2.3e-4 → 1.7e-7。GXII の S_N デッキは 1D の既定の空間離散化が変わったため、吸収率 0.3187 → 0.3144、最大密度 47.42 → 48.98 g/cm³、中心温度の最大 1031 → 870 eV、エネルギー保存誤差の最大 2.2e-4 → 9e-16 となり、FLD デッキの結果（48.66 g/cm³、917 eV）に近づきました。NIF DS デッキ（NIF 直接駆動の設計スタディ。配布物には含めていません）の DD 中性子数は、下記の人工粘性の不具合のため丸め誤差だけで変わっていました（初期温度を 1e-13〜1e-10 変えた 5 本の 8 ns までの中性子数が 1.65〜2.20e5。基準版の 1.995e5 と、この版の修正前の 1.809e5（いずれも 14 ns）もこの範囲の中です）。修正後は 14 ns で 1.2486e5 で、同じ 5 本の 8 ns までの中性子数は 6 桁一致します。流束制限子の評価方式による差は +0.9 %（`limiter_evaluation="iterate"` の方が大きい）です）。あわせて、1D の本番ステップを高速化し（NIF DS の最初 0.2 ns で 1 ステップ 7.5 → 4.2 ms、GXII の FLD デッキの 1.85 ns 以降で 18.6 → 9.9 ms）、1D の run を途中のチェックポイントから再開したときに元の run とビット一致するようにしました。

### 互換性に関わる変更

- **1D の S_N の既定の空間離散化を線形不連続法（`Radiation.sn_transport.spatial_scheme="linear_discontinuous"`）に。** `spatial_scheme` を指定しない 1D デッキの結果が変わります（2D_RZ は従来どおり `"linear_characteristic"`）。以前の 1D S_N のチェックポイントから再開するときは、デッキに `spatial_scheme="linear_characteristic"` を明示してください（従来の方式で続きます）。新しい方式は角度分布（`radiation_sn/psi_prev`、`psi_sd_prev`）とセル内の電子エネルギーの差（`radiation_sn/ee_node_offset`）をチェックポイントに保存します（無いチェックポイントからは等方・一様で始めます）。
- `Laser.raytrace.integrator` の既定を `"auto"` に（1D 球では特性曲線積分、それ以外は従来の行進）。1D の円柱・平板のレイトレースは特性曲線積分でのみ動きます。
- 1D FLD の外側反復: `outer_accel` の既定 `"auto"` は 1D で灰色加速（`"grey"`。収束した解は変えません）。`limiter_evaluation` の既定は `"predictor"`（最初の反復の放射場で流束制限子を評価し、ステップ内で固定）で、これは離散化を変えます（従来の毎反復の評価は `"iterate"`）。NIF DS の 14 ns の DD 中性子数は従来の評価で +0.9 % です（この項の以前の版にあった CFL ごとの比較は、下記の人工粘性の不具合による揺らぎの中の 1 本ずつの比較でした）。
- `Main.dimension="1D_CYL"` は `1D_SPH` と `Mesh.geometry_1d="cylindrical"` に読み替えます（以前の 1D_CYL のチェックポイントも再開できます）。
- **1D の CSW 人工粘性の下限（`Numerics.hydro.csw_shock_limiter_floor`、既定 0.65）を連続な重みに。** 再構成した圧縮率が正かどうかの符号だけで入り切りしていた下限を、Christensen の制限関数 ψ（Caramana-Shashkov-Whalen 1998 の式 12）で (1−ψ) 倍して掛けます（隣のセルが圧縮していない衝撃波の足元・後端では従来どおり下限いっぱい、速度が線形な圧縮では 0 へ連続に下がる）。`av_type` を指定しない 1D_SPH のデッキ（CSW になる）と、CSW の下限を既定のまま使うデッキの結果が変わります（同梱の例では `examples/implosion/gxii_shell_1D.py` と `av_type` を指定しない検証デッキ。NIF DS のデッキも変わります）。`examples/implosion/` のそのほかの GXII の例と CBET の検証デッキは下限 0 を指定しているので変わりません。GXII の FLD 回帰デッキ（`verify gxii_1d_fld_regression`）の黄金値を作り直しました（中心に衝撃波が収束する瞬間の最大密度 36.5 → 72.8 g/cm³、中心温度の最大 905 → 1132 eV、吸収エネルギー・爆縮時刻・最小殻半径・ρR の変化は 3 % 以内）。
- チェックポイントに、1D では `hydro/cv_e`・`hydro/cv_i`・`hydro/cs`・`hydro_flags/cell_is_void` を、1D と 2D で熱伝導の累積統計 `conduction_state/*` を加えました。これらの無い以前のチェックポイントからは従来どおりに再開します（1D は最初に状態方程式の閉包をかけ直す）。
- `Laser.raytrace.lanes_per_ray`（既定 0）を追加。1D の特性曲線積分で 1 本の光線を追跡するレーン数（32・64・128・256）で、0 はバッチの光線数と GPU から自動で選びます（GPU が同時に動かせるスレッド数に全光線 × レーン数が収まる最大の幅、収まらなければ 32）。和の順序がレーン数で変わるため、結果は丸め誤差の範囲で変わります。同じ GPU・同じ光線数なら run の反復はビット一致しますが、別の種類の GPU や別の光線数ではレーン数が変わりうるので、別の GPU とビットで比べるときは値を固定してください。
- `Radiation.sn_transport.grey_preconditioner`（既定 `"auto"`）を追加。線形不連続法の S_N の放出結合 GMRES の灰色前処理を、節点の補正の利得の最大が 0.1 を超える Newton 反復だけで使います（`"on"` で従来どおり常に使う、`"off"` は使わない）。収束した解と許容値は同じで、丸めが変わります。
- この 2 つを含まない以前のチェックポイントから再開すると、新しい既定値（自動）で続きます（以前の run は 32 レーン・前処理ありでした）。

### 不具合修正

**S_N（1D）**

- **線形不連続法を追加し 1D の既定に。** 従来の方式はセル内で放射源・散乱源が一定のため、光学的に厚いセルで誤った流れを作っていました（球、放射源 1 + 4r²、σΔr = 4 で拡散の流れの 3.2 倍）。強度・放射・散乱源・電子温度をセル内で線形に持ち、セル内の温度分布をステップ間で持ち越します。Marshak 波（σΔr = 4、10 セル対 160 セル）の最大温度誤差は球 0.101・平板 0.068（従来 0.695 / 0.595）、ステップのエネルギー収支は 3e-15 以下（GXII の S_N デッキで 2e-16、従来 1.5e-6）。純吸収体の減衰は 2 次精度なので、細いビームの無衝突減衰が支配的な問題には `"linear_characteristic"` の方が正確です。
- **従来方式（linear_characteristic）の流出制限。** 1 ステップの流出を蓄えた放射エネルギーと上流の制限済み流入で抑える際、上流の 1 段目の係数で流入を割り引いていたため、セル幅が cΔt より小さいと輸送が詰まっていました（Marshak slab が 8 セルでは加熱されるが 32・128 セルでは内側が冷たいまま）。流れの順に上流の最終係数で流入を加える形に直し、格子を細かくすると線形不連続法の結果へ収束するようになりました。GXII の S_N デッキ（300 セル、0.15 ns）で最大密度 3.80 → 3.40 g/cm³（線形不連続法 3.37）。
- 散乱の加速（DSA）を掃引の空間離散化と整合する形に（面モーメントの P1 系）。
- history に S_N の反復統計（`radiation/sn_outer_iterations`・`sn_inner_iterations`・`sn_outer_residual`・`sn_converged`）を追加。

**FLD（1D）**

- void セルを真空として扱います（定数不透明度で先頭材料の κ を取っていた、吸収・放射の交換に加わっていた）。GXII で外側反復が収束しないステップ 238 → 85、平均反復 13.6 → 4.0。
- 外側反復の灰色加速（多群-灰色の合成補正、Fleck 因子の温度依存を含む）と、流束制限子を最初の反復の放射場で評価する方式。GXII 回帰デッキの全ステップが 2 次収束（平均 2.6 回、従来は初期 0.2 ns で 20 回の上限に到達）。

**レーザー（1D）**

- **特性曲線積分（`Laser.raytrace.integrator="characteristic"`）を追加し、1D 球の既定に。** 球対称の場で角運動量を保存する光線を区間ごとに求積で積分します（転回点・臨界・中心を厳密に扱う）。行進法の刻み幅による誤差と、転回点近くで刻みが細かくなる負荷がなくなります。臨界面での反射（`critical_handling.terminate=False`）、円柱・平板の 1D 形状にも対応。
- 外向きの区間の変数変換の中心を、転回半径と区間自身の根のうち近い方に（GXII の臨界層でパネル上限に達する区間が 300 step で 63 → 0）。
- 1D の断面に入る光線を断面の内側に置く（丸めで外側に出た入射点が失われていた）。レーザーのパワー台帳を明示的に（未吸収は光線自身の集計、hot-electron の逃散を 1 回だけ数える、転送で止まったパワーと上限で受け入れた求積区間を記帳）。
- ゴーストコロナの幅をレーザーの立ち上がり時刻から測る。ビームの `custom` プロファイル・`radius_um`・`LaserBeam.energy_J`。
- 時間の関数（パルス波形など）を t_end によらない絶対時刻の格子で表にする。

**多材料（1D）**

- 閉包のパラメータ（EOS の方式、`cv_e_override`、`eos_T_ref_eV`）、レーザーの衝突の電荷、電離のモーメントを材料ごとに。
- 多材料の FLD と S_N の不透明度（セルごとの吸収・放射・散乱、材料ごとの NLTE）、不透明度の混合則 `harmonic_mass_R`・`max`、IONMIX の LTE 表、表の不透明度での Fleck の secant/guard β と `exp_phi1`。
- 1D の材料別エネルギー保存（`Numerics.materials.per_material_conservation_enabled`）は引き続き拒否します（理由はメッセージと仕様書に記載）。

**人工粘性・再開・診断（1D）**

- **NIF DS の結果が丸め誤差で ±14 % 変わっていた。** CSW 粘性の下限の入り切りが、輻射で予熱された CH 殻の衝撃波前方の滑らかな圧縮で、丸め誤差程度の差を 1 ステップあたり約 7 % ずつ増幅していました（初期温度を 1e-13〜1e-10 変えた 5 本の 8 ns の DD 中性子数が 1.65〜2.20e5）。下限を連続な重みにして、5 本とも 1.24666e5 になりました（上記）。
- **途中のチェックポイントから再開した run が元の run とビット一致しなかった。** run 開始時の初期音速の準備で、閉包済みの状態に状態方程式の閉包をもう一度かけ、ee・Te・Pe が丸め誤差だけ動いていました。空セル（is_void 材料）の印は保存も復元もされず、空セルを含むデッキは再開後にレーザーの ghost corona が働かず異常終了することがありました。熱伝導の累積統計も 0 から数え直していました。GXII の FLD デッキ（10000 ステップ目から、全物理・熱伝導なし・放射なしの 3 通り）で再開した run の履歴の全列が、GXII の FLD の小デッキ（ghost corona あり、新しい ctest `restart_bitwise_gxii_1d`）で履歴の全列とチェックポイントの全データが、元の run とビット一致します（光線追跡の再利用 `Laser.raytrace_skip_config.enabled=True` の run は、再開直後に全光線を追跡するので一致しません。SPECIFICATION の再開手順 7b）。
- 冷たい標的から始まる run（NIF DS）で、1 ステップ目のエネルギー保存誤差が 3.9 と出ていました（負の冷たい内部エネルギーのため総エネルギーが小さく、入力の 2.6e-18 erg が分母になっていた）。分母をエネルギーの各項の絶対値の和にし（全項が正なら従来と同じ）、1 ステップ目は 1.4e-23、最初の 40 ステップの最大は 4.0e-11 になりました。床・安全補正の比の警告と、平衡状態から始まる run の 2T 崩壊の警告（実行中一度も Te と Ti が分かれなかった場合だけ終了時に出す）も直しました。
- 特性曲線積分の光線軌跡の点数が `Laser.ray_output_max_steps` を超えると、出力時に隣の光線の行（最後の光線では配列の外）を読んでいました。
- `test_1d_sph_bitwise_golden` の黄金値を作り直しました（2026-09-24 の 1T 再規格化の変更以来、失敗していた）。

**エネルギー台帳・その他**

- エネルギー予算と 1T の再規格化で運動エネルギーを節点の形で測る（セル平均の形はアブレーション域で沈着量の 1 割程度過小で、見かけのエネルギー損失を示していた）。
- 1D のエネルギー台帳の浮動小数点の原子操作を固定順の和に（実行ごとにビット一致）。燃焼のモンテカルロの沈着は固定小数点の整数原子操作に。
- メッシュ要件の陽子質量、NLTE の上限クランプ数の材料和、平板の境界の pdV 仕事、HDF5 の `geometry_1d` 属性、凍結デッキへの表の内容の取り込み。

### 性能

特記しないものは結果を変えない高速化です（変更ごとに前後でビット一致を確認）。

- 線形不連続法の S_N: 節点の物質更新と線形化を節点ごとに 1 warp（群の Planck 項をレーンで並列）、P1 系を Newton 反復ごとに 1 回だけ分解、掃引のオペランドを 1 対角先読み。GXII の S_N デッキ（300 セル、80 群、S16）の後半 1593 ステップで放射段 1 ステップ 55.4 → 10.3 ms（従来方式は 14.3 ms）。掃引とブロックの逆行列の前計算は丸めを変えます（同デッキの積分量で相対 2e-5 以内、反復回数は同じ）。
- Planck 分率の温度区間の探索を群のループの外へ、灰色加速のスペクトルを warp 並列に、レーザーのパターン診断の半径を GPU 上で。GXII で 1 ステップ 19.6 → 15.8 ms（nsys 下）。
- 同梱の GXII の例（`examples/implosion/gxii_*.py`）の光線数を 8000 → 1000（1D 球の既定）に。特性曲線積分では 1000 本で 16000 本と同じ結果になり（2.5 ns の吸収率の差 1e-5 程度、最大密度の差 7e-4 以内で光線数に対し単調でない）、レイトレースのカーネルが最初の 300 ステップで 1 ステップ 6.46 → 1.00 ms になります。自分のデッキで 8000 本を指定している場合も同様に減らせます。
- **1D の本番ステップ（この項の変更の前との比較、1 ステップあたり、繰り返し計測の平均）: NIF DS の最初 0.2 ns で 7.51 → 4.18 ms、7 ns 以降で 5.08 → 3.28 ms、GXII の FLD デッキの最初 300 ステップで 6.42 → 5.88 ms、1.85 ns 以降で 18.62 → 9.86 ms。** history の行を書き込み用のスレッドで書く（HDF5 の呼び出しは 1 本の排他で直列化）、スナップショットとチェックポイントの圧縮を並列に、燃焼の LP 分配表を減速項の使い回しと背景の構築で、表の区間探索、低温平衡の電子温度の逆算を表の温度節点で（評価回数 53.5 → 6.4 回、NIF DS の結果は逆算の許容誤差の範囲で変わる）、同じ密度での表の評価の共有、FLD の灰色加速の三重対角解法・光線追跡の断面表・外側反復の退避コピー、光線軌跡の出力の受け取り。ほかは結果を変えません。
- **1D の本番ステップの追加の高速化（上の項の変更を入れた版との比較、1 ステップあたり、2 回の計測の平均）: NIF DS の最初 0.2 ns で 4.50 → 3.91 ms、7 ns 以降で 2.82 → 2.53 ms、GXII の FLD デッキの最初 300 ステップで 5.64 → 4.43 ms、1.85 ns 以降で 9.17 → 7.50 ms、GXII の S_N デッキの最初 300 ステップで 12.08 → 9.30 ms、2.09 ns 以降で 20.82 → 16.10 ms。** 結果が丸め誤差の範囲で変わるもの: FLD の灰色加速の三重対角方程式を並列循環縮約で（GXII で 0.27 → 0.04 ms、後半 0.55 → 0.07 ms）、特性曲線積分のレーン数の自動選択（同梱の例の 1000 本では 32 レーンのまま。250 本なら 128 レーンでトレースが 1.48 → 0.84 ms）、線形不連続法の S_N の灰色前処理を必要なときだけ（GXII の S_N で 2.3〜3.8 ms）、2 温度の状態方程式の閉包と音速をセルごとにイオンと電子の 2 つの warp で（表の逆算の二分探索を warp の 31 レーンで先読み。閉包 0.29 → 0.18 ms、音速 0.14 → 0.11 ms、NIF DS 後半。1 スレッド版と判定は同じで、値はコンパイラの積和のまとめ方の違いだけ変わる）。本番の 3 デッキの結果は、基準版の初期時間刻みを 1e-12 だけ変えたときの揺らぎと同程度の差です（GXII の FLD の吸収エネルギーの差 6e-6 に対し揺らぎ 1e-5、NIF DS の 8 ns の DD 中性子数は 9 桁一致）。結果を変えないもの: 光線追跡の断面節点をセルごとに並列に配置（0.46 → 0.12 ms）、レーザーの台帳の畳み込みと空セルの節点の追従を並列に、スナップショットの圧縮の待ち・チャンクの書き込み・close・公開を別スレッドで（書き込み側は内容を書いて圧縮を依頼したら戻る）、圧縮スレッド数をコンテナの CPU 上限から決める（RunPod の pod では 126 → 11）、流体段の CUDA 呼び出しの削減（上書きされる作業領域のゼロ埋めを省く、旧状態のコピーをまとめる等。NIF DS で 1 ステップ 184 → 112 回）。

### 既知の制限

- **NIF DS デッキの DD 中性子数は格子に収束していません。** 最初の衝撃波が中心に収束するとき、最内のセルの温度が格子の細かさで決まり、DD の反応率がそれに従います（14 ns の中性子数: 既定の格子 1.2486e5、2 倍 1.6466e5、4 倍 2.2687e5、中心温度の最大 818・880・939 eV）。時間刻みには収束します（CFL 0.45・0.225・0.1125・0.05625 で 1.2486・1.2376・1.2290・1.2250e5）。中性子数を比べる計算は同じ格子どうしで行ってください。以前この項にあった「既定の CFL では時間刻みに収束しない」は、上記の人工粘性の揺らぎによるものでした。
- NIF DS は多材料のため互換エネルギー方式（`compatible_energy`）を使えず、立ち上がり（0.045〜0.06 ns）の流体の半ステップでエネルギー保存誤差が最大 3.4e-4 になります（CFL 0.05625 で 2.4e-5。これまでは 1 ステップ目の誤報に隠れていた）。（2026-09-26 に解消。使えなかったのは多材料のためではなく、低温平衡構成則との併用が未実装で、NIF DS の材料のイオン表の極低温の非単調性が起動時の検査に落ちていたためです。2026-09-26 の項。）
- 2D の S_N の流出制限は 2 段の形のままで、1D で直した詰まりと同じ型の制限を持ちます（未検証。2D は今回の対象外）。

### ドキュメント・検証系

- NUMERICS（§5.3.6 特性曲線積分、§6.7 FLD の外側反復、§6.8.2 流出制限、§6.8.4 線形不連続法）、SPECIFICATION、ARCHITECTURE に反映。
- 新しい ctest: `test_sn_1d_ld`、`test_sn_1d_ld_step`、`test_ray_trace_characteristic`、`test_sn_streaming_limiter` の追加分ほか。
- 人工粘性・再開・診断・高速化の分: NUMERICS（§3.1.6 CSW の下限、§10.2・§11 保存誤差の分母）、SPECIFICATION（`csw_shock_limiter_floor`、再開手順 7b）、ARCHITECTURE（history の書き込みスレッド、圧縮の並列化、LP 分配表の背景構築）。新しい ctest `restart_bitwise_gxii_1d` と、`test_artificial_viscosity` の下限の試験 4 件。
- 追加の高速化の分: NUMERICS（§5.3.6 (c') レーン数、§5.7.4 断面節点の並列配置、§6.7 灰色加速の並列循環縮約、§6.8.4 灰色前処理の自動選択）、SPECIFICATION（`lanes_per_ray`、`grey_preconditioner`）、ARCHITECTURE（スナップショットの完了スレッドと圧縮スレッド群、表の逆算の warp 協調形）。新しい ctest: `test_eos_inverse_warp`（warp 協調の逆算と逐次の逆算の比較）、`test_trace_profile_nodes`（並列配置と逐次配置のビット比較）、`test_hydro_1d_step` の `[closure]`（2 warp の閉包・音速と 1 スレッド版の比較）、`test_ray_trace_characteristic` の全レーン数での試験。
- 公開ドキュメント（日英）: S_N（1D の線形不連続法、`grey_preconditioner`）、FLD（1D の外側反復の灰色加速と流束制限子の評価、void セル）、レーザー（特性曲線積分、臨界半径での反射、`lanes_per_ray`、1D の円柱・平板）、流体（1D の CSW の下限、`av_type` の既定）、出力（history の `*_step` と S_N の反復統計、チェックポイントの追加分）、実行（再開のビット一致）のページを更新。

## 2026-09-24

1D コードの精査で見つかった誤りを修正し、結果を変えない高速化を加えました。1D の本番デッキ（GXII と、配布物に含めていない NIF 直接駆動の設計スタディ NIF DS）の結果が変わります（基準版との比較: CD の表を使う GXII デッキで 1 ns の吸収率 0.272 → 0.338、最大圧縮 42.2 → 37.7 g/cm³ で 75 ps 早まる。NIF DS デッキ 14 ns で DD 中性子数 +38 %、中心温度の最大 −4 %、最大密度 +1 %。NIF DS の主な寄与は熱副段での電子・イオン交換の二重適用の解消、Marshak 駆動の区間中点、Thomas–Fermi の電離度、FLD の界面不透明度、表の Fleck 比熱、CFL と Δt 成長の修正）。2D の結果に影響する修正は項目内に「2D も」と記しています。

### 不具合修正

**レーザー（1D）**

- **ビームのパワーをステップの区間平均で評価。** 従来はステップ開始時刻の値を使っていたため、沈着がパルスに対して Δt/2 遅れ、急峻な立ち上がりをまたぐステップはその分を受け取れませんでした。凍結した区分線形の波形の区間平均を厳密に使い、ステップの和は刻み方によらずパルスの積分に一致します（光線のパワー、Langdon の強度、指令エネルギーの台帳、再トレース省略の規格化のすべて）。
- **ビームの強度分布を標的中心を通る面で評価。** 従来は打ち出し面（コロナの膨張とともに外へ動くレーザーメッシュの外半径）で固定幅の分布を評価していたため、コロナが広がるほど外側の光線の重みが下がっていました（GXII で最外の環の重みが 1 から 0.135 まで低下）。
- **臨界セル対をまたぐ n̂ を log 線形で連続に。** 従来は線形値が 1 を下回る点だけを置き換えていたため、交点まで n̂ = 1 に張り付いてから不連続に落ちていました。レーザーメッシュの細分帯も、沈着の分割と同じ臨界半径を中心に置きます。
- **ステップ間の n̂ の平滑化を廃止。** 平滑化した n̂ が動く臨界面の近くで密度より最大約 1 % 臨界側へ遅れ、光線が臨界近傍を遅く進んで実際より大きな吸収係数を見ていました（GXII でレイトレースの時間が約 3 倍、半数のステップで最大ステップ数に到達）。吸収率のステップ間の揺らぎも減らしていませんでした。写像は現在の流体の状態だけの関数になり、平滑化の状態は再試行とチェックポイントからも除きます（古いチェックポイントの `laser_smoothing` は読みません）。
- **ゴーストコロナ（`Laser.lasermesh.ghost_corona`）の密度と幅。** 内側の密度を最外セルの n̂ で上限し（既に ne_min より薄い表面にはゴーストを置かない）、解像されたコロナが育つにつれて薄めます。幅は max(n_out·dR_fine, c_s t) にこの減衰を掛けた値で、c_s は外側のセルの Te・Z̄・A から求めます。既定値を仕様どおり `n_out=12`、`ne_max_frac=0.99` に直しました（同梱のデッキは両キーを指定しているため変化なし）。既知の制限: t はシミュレーション時刻なので、パルスが遅れて始まるデッキではゴーストが広すぎます。
- 真空から物質へ入った点から光線を続ける（従来は表面から最大 1 刻み分の物質を吸収なしで飛ばし、次の刻みで表面の急な勾配による余分な蹴りを受けていました）。
- `Laser.ib.zeff_model="auto"`（1D_SPH）: 全ての非 void 材料が同じ多種組成（TMAT の組成）を持てば、それを種のリストとして逐次剥離の Zeff を使います。従来は電離段の表が無ければ常に `"off"` で、CD の完全電離コロナでは逆制動輻射の吸収係数が 0.66 倍に過小でした。1D_CYL と 2D_RZ では従来どおり `"off"` です。
- 沈着の受け手の探索で、殻に遮られた内側の亜臨界ガスを数えない（ガス充填シェルで、ガスがレーザーの全パワーを受けていました）。
- ビームの向き: 焦点が打ち出し面より上にある場合や、向きが (0, 0, −1) 以外の場合も、標的へ向かって打ち出します。焦点の lab z が同じで向きの違うビームを 1 本にまとめていた誤りも直しました。
- レイトレースを省略するステップ（`Laser.raytrace_skip`、既定で無効）でも、キャッシュした沈着を通常のステップと同じ再分配に通します（void セルに沈着していました）。
- レーザー演算子の直前に Z̄ を現在の ρ・Te から評価し直します（従来は半ステップ前の値）。
- レーザーの注入で表の温度上限を超えるエネルギーを高温側の延長（理想気体の裾）で閉じます。

**輻射**

- **1D FLD の界面の Rosseland 不透明度を、セル中心間の光学的厚さをその距離で割った値に。** 従来の単純平均は、幅比 w の界面で拡散極限の伝導率を最大 (w+1)/2 倍誤っていました。等幅の界面は従来とビット一致です。
- **Planck 群分率の温度補間を、累積分率と裾の ln T についての三次 Hermite 補間に。** 線形補間は Wien 側の分率を節点間で最大約 9 % 過大にしていました（2D も）。
- NLTE の Fleck 因子から、残っていた β ≤ 1 の上限を撤去（理想気体の比熱で代用するセル）。
- 1D FLD で表の不透明度を使う材料の Fleck 比熱を、`fleck_cv_source`（既定 `"table"`）どおり電子表の比熱に。表の不透明度では効かない secant/guard の β と `exp_phi1` の指定は、検証で拒否します。
- 1D の多材料 FLD で η を放射の不透明度から作ります（κ_PE ≠ κ_PA の LTE 表でエネルギーが生成・消滅していました）。
- 1D S_N: power_law と freq_dep_marshak の散乱、体積源の注入量の記帳と物質の Newton 法の括り、沈着の時間レベル、DSA の面積を幾何に合わせる（平面・円筒でも DSA が働く）、逃散エネルギーを群の固定の順で合計（実行ごとにビット一致）、毎回の解を現在の rad_E から始める（チェックポイントから再開した直後に放射場を失っていました）、角度分布 `psi_prev` を再試行とチェックポイントに保存。
- 時間変化する Marshak 駆動の表を、演算子が進める区間の中点時刻で評価します（1D FLD と S_N。熱副段では各副段の中点）。

**流体・状態方程式（1D）**

- **CFL の人工粘性の一次項を、圧縮しているセルだけに加える。** 従来は全セルに加えていたため、圧縮していないセル（静かな燃料、膨張するコロナ）が Δt を決めるとき、音響条件の 1/(1+C₁)（`csw_C1=0.5` で 2/3）に制限されていました。
- 出力時刻に合わせて切り詰めたステップの後も、Δt の成長を切り詰める前の値から続けます（従来は出力のたびに 1 ステップ 20 % ずつ回復していました）。
- 表の温度上限を超えるセルの音速を、閉包の高温側の延長に合わせます（√(T_top/T) 倍小さかった）。
- compatible エネルギー更新で表の符号付きエネルギー（負の cold curve）を保ちます。
- 多材料: 表の閉包を使うかどうかを先頭の材料で決めない（理想気体を先に並べたデッキで、表を持つセルまで理想気体として扱っていました）。表を持たない材料のセルは、他の材料の表を借りずに自分の理想気体で閉じます。
- 1T の注入（レーザー・燃焼・輻射源）を全エネルギーの表で閉じ、イオンの比熱を二重に足していた誤りを直しました（2D の 1T も）。
- `Numerics.radiation_thermal_subcycle` が働くステップでは、電子・イオン交換を熱副段の中だけで行います（流体の両半ステップでも交換していたため、緩和が 2 倍速かった）。
- 熱副段の Qei と燃焼の沈着でも、表の温度上限を超えるエネルギーを高温側の延長で閉じます。
- 1T のエネルギー再規格化で、輻射圧の仕事と床の注入を保ちます。
- 既定で無効の経路: 衝撃波後加熱の競合、`rigid_wall` の Qei、圧力境界の人工粘性の仕事、`midpoint_v2` の予測段の床、compatible の人工粘性の行き先（`av_heat_to`）。
- 1D ALE（既定で無効）: 速度の射影を half-index-shift 法に（一様な速度は一様のまま、節点の運動量を保存）、remap を確定した後の EOS を流体と同じ閉包で、発火条件と候補の判定をパラメータ名どおりに（新キー `Numerics.ale1d.emergency_max_dr_ratio`、既定 1.25）。
- 伝導の後のエネルギー記帳に、解で使った比熱を使います（表の比熱が 0 以下のセルで記帳が漏れていました）。

**材料・伝導・燃焼（2D も）**

- **Thomas–Fermi の平均電離度を More (1985) Table IV のフィットに。** 旧式は Z > 1 の冷たい物質を過大に電離させ（CH 1.05 g/cm³、0.1 eV で 1.85、正しくは 1.30）、高温で完全電離へ近づくのが遅い（D₂ 0.17 g/cm³、1 keV で 0.946、正しくは 0.997）ものでした。`Materials.zbar.model="thomas_fermi"` の全ての実行（1D・2D）で結果が変わります。
- **2D の Spitzer 係数と材料別の伝導経路にも ξ(Z)（Epperlein–Short）を適用。** 2D で Z ≠ 1 の Spitzer 伝導は ξ(Z) 倍（Z = 3.5 で 2.04 倍、Z = 79 で 3.99 倍）になります。1D の主経路は適用済みでした。
- 材料別伝導のエネルギーの二重記帳を解消。材料別の保存（`Numerics.materials.per_material_conservation_enabled`）は 2D_RZ だけで受け付けます（1D では古い材料別エネルギーでセルのエネルギーを上書きしていました）。
- 燃焼: Corman の電子エネルギー緩和時間に論文どおりの因子 8（高速生成物の電子加熱率が半分だった。`Burn.scheme="diffusion"`（1D・2D）と `"mc"`）、この 2 つの scheme で反応段の Δt 制限を保つ、荷電生成物の減速と局所飛程にセルのイオン組成を使う（D₂ 燃料では DD 生成物の飛程が短くなる）、1D の複数 rank で入力を集めて台帳を一度だけ数える、沈着の無いセルを閉じ直さない、中性子の熱幅（診断）が半分だった。
- `Materials.opacity_mix_rule` は実装済みの `"linear_mass"` だけを受け付けます（他の値は受理したうえで無視されていました）。

**結合・再試行・出力**

- 熱副段の床の再試行で、比熱・Z̄・床の台帳・伝導と S_N の再試行要求も元に戻します。熱副段に分けたステップの `rad_dep`・`rad_emit`（`deposited_power`）を全副段の和にします（最後の副段の分だけでした）。
- cold_equilibrium の参照エネルギー e_cold を全ステップの再試行で巻き戻し、再開時に復元します。
- **history のエネルギー台帳（`energy/marshak_in`・`pdv_boundary`・`numerical_loss`・`floor_injected`・`safety_injected`・`solver_residual`・`E_volume_in`）を仕様どおり実行全体の累積値に。** ステップごとの値は `*_step` の系列へ移しました。系列を足し合わせていた読み手は、`energy/marshak_in_step` の有無で新旧のファイルを判別してください（同梱の解析スクリプトは対応済み）。
- 1D の history: dt 内訳の群は毎ステップ、他の系列の行は `Output.history_every` の間隔で書きます（従来は dt 内訳が有効だと毎ステップ全系列を書いていました）。毎ステップの行が必要な同梱デッキには `history_every=1` を明示しました。
- 既定で無効の persistent loop（`Numerics.persistent_loop`）: 実装していない設定は拒否して通常の経路で走らせ、受け付けた設定は通常の経路と一致させます（Δt、光線の初期化、1T の閉包と再規格化、台帳、history、NaN 検査、終了処理）。

### 性能

以下は結果を変えない高速化です（変更ごとに前後でビット一致を確認）。RTX 4090 での基準版との比較（物理の修正でステップ数も変わるため、実行全体の時間には両方の効果が入ります）:

- **NIF DS（3.5 ns）: 46.1 s → 31.9 s（ステップあたり −30 %）。** デバイスを待つ API 呼び出しが 1 ステップ 162 回から 32 回に、カーネルの起動が 318 回から 218 回に減りました。
- **GXII（CD の表とレイトレース、2.5 ns）: 631.6 s → 615.9 s。** レーザーの修正でレイトレースのカーネルがステップあたり 22 % 長くなった分を、ホスト側の待ちの削減（1 ステップ 101 回 → 38 回）とステップ数の減少で打ち消しています。
- 主な変更: history の一括追記でファイル全体を複写しない、1D の history の行を `Output.history_every` の間隔で書く、1D の Lagrange ステップ・伝導の陰解法・FLD の Anderson 混合・熱副段・Qei 副段のホスト同期を 1 回以下に、NLTE 不透明度の表の対数を事前計算、セルごとの EOS カーネルを小さな格子では 1 ブロック 1 warp で起動（NIF DS の 2T 閉包 3.10 → 1.54 ms/step）、エネルギー総和のリダクションを 1 回の起動に、1D ALE の作業配列をプールから確保、1D S_N で散乱が無いときに同じ掃引を繰り返さない。

### ドキュメント・検証系

- gxii_1d_fld_regression の golden を再基準化しました（rho_peak_max +26.6 %、rhoR_max +15.5 %、E_laser_absorbed +6.6 % ほか）。変化の大部分は臨界セル対の n̂ の連続化と FLD の界面不透明度によるものです。
- CBET の GXII 傾向試験（`cbet_gxii_tendency`）のクランプ回数の目安を、レーザーの修正後の計測に合わせて再設定しました（1 行 512 回・合計 8192 回。レーザーの立ち上がりの 0.1 ns より後のクランプは不合格とする条件を追加）。真空から物質へ入った点から光線を続ける修正などで、立ち上がりの 16 ps 以内に収束途中の反復で起きるクランプが増えましたが、最終パスで補填した電力は 0、台帳の残差は 1e-16 台で、off/on/detuned の吸収の傾向も成り立っています。同じ試験の時間上限も 3600 → 5400 秒に（RTX 4090 で単独 3492 秒、基準版は 3176 秒）。
- 原子操作の台帳（`docs/determinism/atomic_site_allowlist.tsv`）を更新。
- NUMERICS・SPECIFICATION・ARCHITECTURE・CUDA_KERNELS と公開ドキュメント（日英）に各修正を反映し、修正ごとに単体テストを追加しました。

## 2026-09-23

### 機能追加

- **Studio に流体の「T_start 未満のセルの扱い」（`Numerics.hydro.T_start_inactive_cells`: passive_fill / rigid_wall / cold_equilibrium）、電子・イオン交換の比熱（`Numerics.hydro.qei_heat_capacity`: ideal_gas / table）、cold_equilibrium の数値パラメータ（`Numerics.hydro.cold_equilibrium`）、材料ごとの参照状態（`eos.cold_reference`: ρ0・Te0・Ti0・P0・K0）の入力欄を追加。** 物理タブで cold_equilibrium を選ぶと比熱が table になり、材料タブの参照状態にその材料の最初の領域の密度と温度が入ります（P0 = 0、K0 は材料ごとに入力）。フォームの検査はソルバーと同じ条件（1D 球・2T・T_start > 0・全材料 TMAT・参照状態の全項目）です。既定設定のデッキの namelist は変わりません（新しい 2 キーは既定値と異なるときだけ出力するので、2026-09-14 より前のバイナリでも既定のデッキはそのまま動きます）。デッキ取り込みも新しいキーに対応し、フォームで表せない組み合わせ（SESAME の材料で cold_equilibrium など）は元の設定を保持します。

### 不具合修正

- **Studio のデッキ取り込みで、元デッキが既定値を明示したキー（例: `T_start_inactive_cells="passive_fill"`）をフォームで変更すると衝突エラーになっていた。** フォームが書き出さなかったキーを取り込み時に記録し、そのキーの欄を編集したときはフォームの値で置き換えます。ブロックの繰り返し呼び出しなど、より広い範囲で保持している設定との衝突は従来どおり報告します。
- **Studio マニュアルが流体開始温度 T_start を「開始時刻」と記載していた。** 電子温度の閾値 [eV] に修正し、流体グループの入力欄が日英どちらの表示でも枠内に収まるようにしました。

## 2026-09-16

### 機能追加

- **低温平衡構成則 `Numerics.hydro.T_start_inactive_cells="cold_equilibrium"`（材料キー `eos.cold_reference`、数値パラメータ `Numerics.hydro.cold_equilibrium`）。** 表の極低温側の欠陥（cold curve の平坦化・張力・スピノーダル）に依らず、指定した参照状態を全圧 P0・体積弾性率 K0 の力学平衡に置き、電子温度が `T_start_eV` を超えると厳密に元の表へ戻ります。冷たい領域は剛体ではなく通常どおり圧縮・衝撃波に応答します。1D_SPH・2T・表 EOS 専用で、`Numerics.hydro.qei_heat_capacity="table"` が必須です。電子表に付けた cold branch は hydro 閉包・FLD/S_N・伝導・Qei・注入の全評価器に同じ写像で効きます。`state.ee` は電子熱量座標 q_e、`hydro/e_cold` に力学エネルギー C(v) を出力し、エネルギー台帳は q_e+C を集計します。起動時に材料ごとの初期全圧と最大不整合を報告します。NIF DS デッキの既定はこのモード（液体 D2 K0=1.2e9、ポリスチレン K0=5.8e10 dyn/cm²、遷移 1–2 eV）。既定経路（gxii / cbet の回帰）は bit 一致。

### 不具合修正

- **hydro の開始閾値 `Numerics.T_start_eV` が builder を経由しない状態確保でも同じ意味を持つように統一。** `T_start_inactive_cells="cold_equilibrium"` では、どの経路で状態を確保しても hydro の開始マスクは作られません。

## 2026-09-15

### 不具合修正

- **FLD の Fleck 線形化（1D_SPH / 2D_RZ）: 物質更新の再放射項が放射側と違う不透明度を使っていた。** 放射の E 方程式は実効散乱を \((1-f)c\sigma^{PA}E^n\)（吸収不透明度）で戻すのに、物質側の Newton 残差と `rad_emit` 記帳は 2026-07-03 以来 \(\sigma^{PE}\)（放射不透明度）を使っていました。\(\sigma^{PE}\ne\sigma^{PA}\) の TMAT 表では毎反復 \((1-f)c(\sigma^{PE}-\sigma^{PA})E^n\Delta t\) のエネルギーが消え、冷たいセルの外側反復が 2 周期で振動していました。1D の物質更新・persistent loop 版・2D_RZ の物質 Newton を \(\sigma^{PA}\) に統一（灰色・定数不透明度では算術不変）。単体テスト（`test_fld_fleck_beta` の blend ケース）を追加（NUMERICS §6.7 の注記を訂正）。
- **1D hydro の compatible energy 更新で、自由境界の ghost Q が 2026-08-04 以降 0 になっていた。** 運動量式は境界セルの Q をゼロ勾配コピーした ghost を使うのに、エネルギー更新側だけ ghost が 0 になり、自由境界セルに人工粘性があると毎ステップ \(Q_{N-1}A\bar u\Delta t\) のエネルギーが生成されていました。`test_hydro_1d_step` の VNR 試験の失敗を bisect で特定し、FREE 分岐を復元。影響は `compatible_energy=True` かつ `boundary_1d="free"` の run のみ。
- **符号付き表エネルギーの許容域を 1D の全評価器で統一（energy_authoritative モード）。** cold curve が負の表エネルギー（PROPACEOS 由来表の低温側）を、伝導の増分・閉包の修復・FLD の末尾経路・Qei・注入の床分岐が 0 へ切り上げ／床へ埋め戻していたため、冷たい多材料デッキ（液体 D2 + CH）で静止セルのイオンエネルギーが毎サイクル生成されていました。許容域を \([e(\rho,T_{\rm floor}),\infty)\) に統一し（修復は非有限のみ、`floor_clamp` の拒否、Qei は両方向を床上エネルギーで制限、注入の床分岐は温度のみ）、`qei_multiplier` を hydro カーネルにも配線。legacy モードの算術は不変。
- **多材料の 1D 閉包が先頭材料の表に固定されていた。** hydro の EOS 閉包・音速・2T エネルギー更新、輻射の物質更新、Qei、レーザー／燃焼の入射、伝導の再閉包、初期化を各セルの支配材料の表で閉じるようにしました。表の密度下限未満は理想気体へ切り替えず最下行へクランプ。再閉包の表天井キャッシュは Config の表で鍵付け。
- `Materials.materials[].opacity.tmat_skip_lte_repair` が namelist 検証時の変換にしか効いておらず、実行時の表再読み込み（FLD 1D/2D・S_N 1D/2D・IMC・persistent loop・硬 X 線診断）では常に修復が適用されていました。全読み込み点へ配線。
- 表 EOS の音速テスト（`test_hydro_table_eos`）の期待値を、2026-07-26 の解析的局所微分（log 双線形補間の微分は格子節点で 1 次誤差）に合わせて更新（節点 5 %、log 中点 1 % の 2 段検査）。実装は不変。
- **IMC 用の Fleck 因子下限による Δt 制約（`Numerics.dt.f_min_fleck`）が FLD / S_N の run にも掛かっていた。** NUMERICS §2.2 (c) の根拠は Monte Carlo の分散悪化で、FLD 側は f に下限を設けない仕様なのに、駆動側は輻射が有効なら常にこの Δt を評価していました。表 EOS の電子熱容量が表の床にある冷たく光学的に厚いセル（SESAME の液体 D2、25 meV）では β が発散して Δt が 1e-21 s に落ち、計算が進まなくなります（NIF DS デッキ、Tr = 55 eV）。`Radiation.mode = imc_ddmc` 以外では評価しないようにしました（persistent loop の複製も同じ）。1D FLD の既定経路（gxii / cbet の回帰、Marshak 波、灰色 FLD の輻射衝撃波・球面 5 % 摂動、Hammer–Rosen）は状態量が bit 一致で、変わるのは履歴ファイルの診断列 `diagnostics/dt_breakdown_history/dt_rad`（候補値が +∞ になる）だけです。
- **SESAME 表の T = 0 等温線（cold curve）が捨てられ、低温状態が最初の正の等温線にクランプされていた。** 5263 重水素は 290 K（0.17 g/cc で P = 2.9 kbar）、7592 ポリスチレンの電子表は 2901 K が最低行のため、1 meV で初期化した液体 D2 と CH の界面に 2.9 kbar の圧力差が生じ、0.5〜2 ns で D2 側の密度が 12 % 下がり CH 側が 11 % 上がっていました。cold curve を保持し、最初の等温線との間を T について線形に補間する 12 本の合成行を置きます（`Materials.materials[].eos.sesame_cold_curve_rows`、0 で従来挙動）。
- gxii 1D FLD 回帰の golden を、文書化済みの既定変更（Langdon 既定 ON など）の後に再基準化。

### 機能追加

- **1D FLD 外側反復の Anderson 加速。** `Radiation.multigroup_diffusion.outer_accel="anderson"`（`anderson_m`, `anderson_beta`）を 2D_RZ から 1D_SPH へ移植（`src/radiation/fld_anderson.cuh`、有効時は外側ループのパイプライン化を使わない）。既定 `"none"` は従来どおり。Planck 平均不透明度の温度依存が急な冷たいセルで逐次代入が 2 周期に落ちる場合の対策で、NIF DS デッキ 3 ns（1572 サイクル）の未収束を 0 にしました。単体テスト `test_fld_anderson`。
- **`Materials.materials[].opacity.tmat_kirchhoff_pe`（既定 False）。** True で TMAT 読み込み時に全ノード・全群で \(\kappa_{PE}:=\kappa_{PA}\)（Kirchhoff の法則）。PROPACEOS 由来の表は各群の Wien 裾で \(\kappa_{PE}\) が 1e-99 へアンダーフローしたり \(\kappa_{PA}\) の 10³ 倍になったりして Fleck 因子が数 meV で桁跳びし、外側反復が収束しません。LTE 表ではこれを True にします（`is_lte` 属性は変えず値だけ置換）。
- `Numerics.hydro.qei_heat_capacity`（既定 `"ideal_gas"` = 従来の算術、`"table"` = 表の \(c_{v,e}, c_{v,i}\) で Qei を計量）。表の低温電子比熱が理想値より桁で小さいとき、理想計量の Qei が \(T_e\) を過大に動かして振動する問題への対応。
- `Numerics.hydro.T_start_inactive_cells="rigid_wall"`（既定 `"passive_fill"` は不変）: セル単位の hydro 開始温度で非活性のセルを剛体壁として扱う変種。非活性セルはプラズマ端と一緒に平行移動しません。
- **張力カットオフ `Numerics.hydro.pressure_tension_cutoff`（既定 False）。** True で 1D の各 EOS 閉包の直後に、全圧 \(P_e+P_i\) が `pressure_tension_cutoff_value`（既定 0、\(\le0\) [dyn/cm²]）を下回るセルの両種の圧力を同じ比で縮めて全圧をその値にします（値 0 なら両種とも 0 で、床付き相では pdV 仕事をしません）。流体は表 EOS の cold curve の張力を支えられません — PROPACEOS 由来の液体 D2 表は初期状態（0.17 g/cc、1 meV）で −7 kbar かつ \(\partial P/\partial\rho|_T<0\) で、D2/CH 界面の膨張と燃料内部の密度波（丸め誤差の指数成長）を駆動していました。温度・エネルギーは変えず、力・仕事・人工粘性は同じ床付き圧力を読みます。NIF DS デッキの 3 ns 検証で界面変位 0.044 µm → 0、燃料内部の密度摂動なし。単体テスト `test_hydro_1d_step`（tension ケース）。
- **起動時診断: 初期状態の力学的安定性。** 各セルの支配材料のイオン表+電子表から \(\partial P_{\rm tot}/\partial\rho|_T\) を評価し、非正（スピノーダル、または非物理的な cold curve）のセル数・\(\rho, T_e\) の範囲・最小値を WARNING で報告します（状態は変えません）。床温度に固定されたセルの応答は等温で、そこでは Lagrange 格子が丸め誤差を指数的に増幅します（NUMERICS §3）。
- 1D FLD: 放射エネルギーの保存的メッシュ移流 `hydro_coupling="conservative_advection"`（opt-in、1D Lagrangian のホスト駆動ループ）。
- 等質量の自動分割 `Mesh.auto_regions`（等厚の区画を等セル数で分割し、材料境界で質量比を合わせる）。NIF 直接駆動の設計スタディ（`examples/nifds/`）は開発用ワークツリーのパスと未同梱の表に依存するため、このスナップショットには含めません。
- Studio: ヘッダ無し手書き Python デッキの取り込み（⌘O、作業ディレクトリ・環境・サーバ側実行の設定を記録、失敗時に設定を表示）、サーバのファイルブラウザに上の階層へ戻るボタン。
- TMAT: 検証済み wide D2 表のデータ監査（`tools/tmat`; 表本体は同梱しません）。

### 性能

- 1D のデバイス常駐化: ホスト側で評価していた物理（レーザー入射、伝導の再閉包、表電離、放射の投機的スナップショット）をデバイスへ移し、毎ステップの D2H を減らし、history をデータセット単位で一括書き込み。実験デッキで 6.5 ns 実行が −22.6 %、GXII −50 %、Noh −79 %（RTX 4090）。opt-in の NVTX 範囲でステップ内の遅延を帰属できます。

### ドキュメント・検証系

- NUMERICS: 符号付き表エネルギーの許容域（§2 追補）、多材料閉包の支配材料表、Fleck 再放射項の訂正と TMAT 表の \(\kappa_{PE}\) 異常（§6.7 注記）、自由境界 ghost の復元（§3）、初期状態の力学的安定性（§3）、D2 表とメッシュの保存監査。SPECIFICATION §6.4／§9.1 に新キー。
- テスト: 上記の各修正に単体テストを追加（Fleck blend、Anderson、TMAT kirchhoff、多材料の再閉包、伝導増分の恒等性、sub-floor 閉包の拒否、Qei の上限と multiplier、燃焼ゼロ注入の恒等性、剛体壁の非活性セル）。`test_hydro_1d_step` と `test_hydro_table_eos` は全件合格に復旧。
- 格子収束キャンペーン（2026-09-03/04）: 参照表の来歴と較正キーを追記し、要求モデルの既定をキャンペーン結果から較正。

## 2026-09-03

### 機能追加

- **物理由来の初期メッシュ分解能要求（1D、実験的）。** `Mesh.resolution_requirement` により、デッキのレーザー波形・波長・材料層・幾何からアブレート帯の面密度質量天井プロファイル・衝撃波分離天井・層あたり最小セル数を決定論的に見積もり、`validate` と run 開始時に判定します（`apply="report"` 既定はメッシュ不変、`apply="enforce"` は `zoning_intent` に推奨帯を注入／他形式は違反時に拒否）。run 出力に `mesh_requirement.json`、`validate --mesh-preview` に全 1D 形式の節点列と要求・判定を追加。アシスタントの `lint-deck` は要求違反を hard lint として反復フィードバックし、`generate-deck` はプロンプトに要求を提示、`zoning-report` は probe 実測との比を報告します（NUMERICS §3.1.0c、設計 docs/design/mesh_resolution_requirement_20260903.md）。
- **格子収束の実測キャンペーンと較正。** `tools/validation/mesh_convergence_campaign.py`（29 ケース×表面面密度質量の梯子、観測量抽出と収束判定、参照表出力）を追加し、その結果（`docs/validation/mesh_convergence_reference.md`、`tools/assist/data/mesh_convergence_reference.json`）から `Mesh.resolution_requirement` の既定を較正（`zones_per_scale_length` 9、強度補正 `intensity_exponent` 0.4）。
- **`dr_min` と要求天井の衝突検査。** 要求 JSON に帯ごとの `width_max_cm` と `ablation.dr_min_admissible_cm`（注入天井と両立する最大の `dr_min`）を追加。`apply="enforce"` で `zoning_intent.dr_min` がこれを超える場合は求解前に `MESH_RESOLUTION_REQUIREMENT_DR_MIN_CONFLICT` で拒否します（メッセージに許容値を明記）。アシスタントの要求サマリ・生成ガイド・スキルにも反映。
- `tools/assist`: new `docmap` verb (deterministic document map + namelist key index from the `enforce_known_keys` lists in `src/core/namelist/builder.cpp`) and `ask` verb (question answering about TENRYU from the checkout's documents and source, with automatic checks that cited paths and namelist keys exist). New role `question_answering`; read-only provider examples in `tools/assist/assistant.example.toml`. Each user runs it with their own CLI login or API key.
- Skill `tenryu-docs-qa` (`tools/assist/skills/codex/`, `.claude/skills/`) — the retrieval guidance used as the prompt head.
- Studio: question panel in the assistant view (local `assist.py ask --json`, journal progress, evidence check display).
- Export now includes `docs/site/`, `docs/OUTPUT_SCHEMA.md`, `docs/POSTPROCESSING.md`, and `.claude/skills/tenryu-docs-qa/`.

## 2026-09-02

### 機能追加

- **TENRYU Studio に LLM アシスタント統合 (実験的)。** 左ナビの新ビュー
  「アシスタント」から、同梱の実験的アシスタントハーネス `tools/assist/` を
  GUI で操作できるようになりました。
  - **デッキ生成 (LLM)**: 自然言語の仕様から 1D デッキを生成し、サーバーの
    バイナリでの validate/freeze と lint フィードバックを反復します
    (`generate-deck` ループ)。モデルが推測せず質問した場合 (UNCERTAIN) は
    回答して再生成できます。受理デッキはその場で検証・保存・実行可能で、
    実行は通常の実行履歴に入ります。生成はローカルマシンで実行され
    (プロバイダ CLI とその認証はローカル前提)、デッキ検証だけを
    `tools/assist/tenryu_remote.sh` 経由でサーバーに委ねます。既定は無効
    (assistant.toml で opt-in、`TENRYU_ASSIST_DISABLE=1` が常勝の kill
    switch)。全 LLM 呼び出しは `~/.tenryu/studio-assist/<stamp>/journal.jsonl`
    に記録されます。
  - **詳細 Lint**: 検証パネルから、現在のフォームデッキに対しメッシュ・
    ゾーニング系 lint (節点単調性・隣接幅比・autozone 質量比・intent ピン)
    を実行できます。
  - **実行診断**: 実行履歴の終了 run から、ダイジェスト (履歴系列の縮約・
    派生指標)・ゾーニング診断 (アブレート帯の質量形 lint)・ゾーニング昇格案
    を表示できます (サーバー側に python3 と tools/assist を含む
    チェックアウトが必要。ダイジェストの系列縮約には h5py)。
  - これに伴い Studio の Tauri shell 許可へ ssh/scp と並んで bash
    (ローカル実行) が追加されています。GUI マニュアル (gui/manual/) に
    「アシスタント」章を追加しました。
- `tools/assist/tenryu_remote.sh` が `TENRYU_REMOTE_SSH_OPTS` /
  `TENRYU_REMOTE_SCP_OPTS` (および標準の `RSYNC_RSH`) でポート・鍵指定等の
  接続オプションを受け取れるようになりました。未設定時の挙動は不変です。

### 不具合修正

- **配布物に `gui-common/` が含まれず GUI がビルド不能だった問題を修正。**
  Studio (gui/) は共有パッケージ `@tenryu-common` (gui-common/) を参照します
  が、これまでのスナップショットには同梱されていませんでした。今回から
  `gui-common/` と `ops/gui/` (実行スクリプトの同期テストが参照) を同梱
  します。

## 2026-08-31

### 不具合修正

- **FLD の真空拡散極限で行列が発散し NaN を生成する問題を修正。**
  `radiation.multigroup_diffusion.opacity_floor` の既定値を
  `1.0e-100` から `1.0e-6` /cm（平均自由行程 10 km）に引き上げました。
  void セル（一様放射場かつ不透明度がゼロ近傍）では flux limiter が
  拡散係数の発散を止められず、三重対角行列の係数が ~1e98 に達して
  ソルバが NaN を生成し、その NaN が下限クランプで黙って床値化されて
  長時間潜伏したのち流体セル反転として噴出していました。勾配領域では
  limiter により床は物理へ影響しません（Su-Olson 等の光学的に厚い
  検証系はビット一致のまま）。
- **多材料デッキで 2 番目以降の材料の不透明度指定が無視される問題を修正。**
  1D FLD は先頭の非 void 材料の κ を全セルに適用していました。セル毎の
  体積分率混合（2D_RZ と同方式）を 1D にも配線し、あわせて namelist
  検証を強化しました（1D 多材料×放射で不正な不透明度モデルは
  ConfigError になります）。
- **レーザー沈着の会計不整合を修正。** 全節点が臨界密度超のレーザー
  メッシュセルで吸収電力が計上のみされ沈着から漏れていた問題
  （最近接の臨界未満セルへ再配分するようフォールバックを追加）、
  および履歴データセット `energy/radiation_escaped` が累積値ではなく
  ステップ値を記録していた問題（累積化。ステップ値は
  `energy/radiation_escaped_step` に保存）を修正しました。
- **FLD ソルバ出力の非有限値検出を常設化。** ソルバ解の NaN/Inf を
  発生ステップで計数し、`Numerics.safety.nan_fatal=True`（既定）なら
  即時停止します。従来は下限クランプで黙って洗浄されていました。
- S_N の拡散形分母（DSA 前処理・AP ブレンド面流束）に 1e-6 /cm の
  正則化を追加しました。評価下限（`sn_transport.opacity_floor`）は
  減衰検証を壊さないため 1e-100 のままです。

### 機能追加

- **多材料デッキの材料別テーブル不透明度（1D FLD）。**
  各非 void 材料が自分の tmat テーブル（LTE / NLTE いずれも）または
  定数 κ を持てるようになりました。純セルは支配材料（体積分率最大）の
  テーブルを (ρ, T_e) 補間で評価し、**混合セルは寄与材料すべてを厳密
  合成します** — 質量分率重み w_m（無ければ体積分率）で
  σ = ρ Σ w_m κ_m、各材料のテーブルはその部分密度 ρ_m = ρ w_m/f_m で
  評価。NLTE テーブルは放射率≠吸収率を含めて材料別に扱われます
  （NLTE が支配のセルは支配材料近似）。恒久回帰テスト
  `test_fld_multimat_opacity` を同梱（テーブル入替 A/B・定数経路整合・
  混合セル厳密値）。
- **1D レーザープラズマ例題集 `examples/laser_plasma_1d/`（10 題）。**
  箔の衝撃波突破・二段パルス・インピーダンス整合・爆発箔・Kr 中の
  放射性衝撃波・円筒ライナー・中実球収束（3 解像度の収束スタディ付き）・
  D2 充填シェル爆縮・CBET+ホット電子コロナ・Marshak 駆動 DT 燃焼。
  各デッキに実測のコミッショニング指標と注意点を README に記載。
  ※ `.tmat.h5` テーブルはライセンス由来データのため同梱していません。
  本例題集の全デッキは固体材料の EOS に tmat テーブルを用いるため、
  実行には PrOpacEOS/SESAME からのテーブル生成が必要です
  （`tools/tmat/propaceos_to_tmat.py` 参照）。
- S_N 1D の外部体積源（`Radiation.volume_source_rate`）を検証完了し
  実験扱いを解除しました。物質側エネルギー残差に注入項が欠けて
  線源セルの電子温度が床に張り付く欠陥を修正し、独立な S₈ 離散化
  参照（`tools/su_olson_sn_reference.py`、同梱）と 0.2%/5.3%/13.9% で
  一致します。あわせて S_N の外側反復が物質基準を
  反復値に鎖結して弱結合の放射-物質交換を反復回数倍に過大適用する欠陥
  （体積源の反復毎再注入を含む）を、基準の step 開始固定で修正しました。
- レーザー転送の保存監査（環境変数 `TENRYU_LASER_TRANSFER_AUDIT=1`）と
  FLD ソルバ健全性検査（`TENRYU_FLD_SOLVE_CHECK=1`）を追加しました。

### ドキュメント

- SNB 非局所電子熱輸送の適用範囲記述を実装に一致させました
  （1D_SPH と 2D_RZ の両方で利用可能です。旧記述の「1D_SPH は
  ConfigError」はブランチ統合前の状態でした）。ほか、多材料テーブル
  不透明度まわりの実装状態注記を各所で現行化しました。

### 検証系

- 放射衝撃波検証ハーネス（`tools/validation/run_i1_fld_ced.py`）を、
  格子毎の定常相 run から前縁メトリクスを採る方式に修正しました。
  従来は自由境界からの擾乱が前縁へ到達した後の非定常状態を測って
  恒常不合格になっていました（前縁ピークの収縮判定も、固定の
  非平滑参照ピークに対する判定へ再定義）。
- `verify su_olson` を再基線しました。旧参照は退役した Monte Carlo
  モード用の離散座標輸送解で、現行の既定 FLD とは比較対象が不一致
  でした。デッキを FLD（limiter 無効 pin）へ現代化し、同一モデルの
  独立差分解（`tools/su_olson_fld_reference.py`）を参照とする実装検証
  ゲートとして PASS します。
- `verify marshak` のデッキと前提条件を現行 namelist 仕様に更新しました
  （ゲート自体は周波数連続輸送ベンチマークのため、多群 S_N 移植までは
  不合格を許容として文書化）。
- レーザー付き FLD 生産ゲート（I1 radial）を会計修正の上で PASS に
  戻しました。

## 2026-08-28

- `tenryu_lib` をテスト同梱ビルドのときのみ生成するようゲートしました
  （スナップショット構成では 264MB の共有ライブラリを作りません）。
- BUILD.md: CMake 要件 3.24→3.27、GPU アーキテクチャ自動検出の説明を
  追記しました。
- 出荷ヘッダ 1 件の内部文書引用を平易な記述に置き換えました。

## 2026-08-26

- 初回公開スナップショット（ソース+GUI+docs 一式、160 ファイル）。
